
export type IMake = {
  make: string,
  checked: boolean
}

export type IFuel = {
    fuel: string;
    checked: boolean;
};

export type ILocation = {
    location: string;
    checked: boolean;
};

